- name: example - extra_vars
  hosts: "{{ host }}"
  gather_facts: no
  tasks:
    - name: check hostname
      command: hostname
      register: res

    - name: print previous result
      debug: msg={{ res.stdout }}